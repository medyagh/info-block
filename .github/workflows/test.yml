name: CI

on:
  pull_request:
  push:
    branches: [main, master]

jobs:
  info-block:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest          # x64
          - ubuntu-24.04       
          - ubuntu-22.04       
          - ubuntu-22.04-arm       # arm64
          - ubuntu-24.04-arm       
          - macos-14               
          - macos-latest           
          - windows-latest         # x64
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run info-block action
        uses: ./
